extends layout

//-
    index.pug variables
    ********************
    @layout.title {String} title of current page
    @header.layout.isAuthenticated {Boolean} shows login/logout button

block content
    include ./partials/question_form.pug

    div(class="container-fluid" id="threads")

    include ./partials/popup_error.pug

block scripts
    script(src="/socket.io/socket.io.js")
    script(src="/js/socketModule.js")
    script(src="/js/main.js")