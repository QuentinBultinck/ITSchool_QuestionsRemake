extends layout

//-
    index.pug variables
    ********************
    @layout.title {String} title of current page
    @layout.isAuthenticated {Boolean} shows login/logout button

block content
    h1 Welcome to #{title}